# Go for the backend

The frontend uses React + TypeScript, so a shared language (TypeScript everywhere) was the obvious default. We chose Go instead because: the team wanted to challenge themselves with an unfamiliar backend language, Go enforces an explicit API contract via OpenAPI rather than relying on shared types, and the hackathon's learning goals outweighed the convenience of a single language stack.
